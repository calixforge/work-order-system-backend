# 项目根 lombok 配置
config.stopBubbling = true
# 让 Lombok 把字段上的 @Qualifier 复制到 @RequiredArgsConstructor 生成的构造器参数上;
# 不加这行,字段级 @Qualifier 对构造器注入完全不生效(静默无效)
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
